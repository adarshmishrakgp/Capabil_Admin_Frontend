NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_SITE_URL=https://capabiliq.com
# true also accepts the seeded demo credentials when the API is unreachable, so
# the login screen can be explored on its own. Every screen still needs the API.
NEXT_PUBLIC_USE_MOCK=false
